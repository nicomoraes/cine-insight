import NotFoundContent from '@/components/common/NotFoundContent';

export default function NotFound() {
  return (
    <NotFoundContent description='Não foi possível encontrar o recurso solicitado.' />
  );
}
